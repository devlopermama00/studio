# The base URL of your deployed application.
# This is used for generating absolute URLs for things like password reset emails.
# You can get this from your browser's address bar when viewing your app preview.
NEXT_PUBLIC_APP_URL=https://<your-app-url>.web.app

# MongoDB Connection String
# Get this from your MongoDB Atlas dashboard.
# IMPORTANT: Make sure to include the database name at the end of the URI (e.g., /tourvista)
MONGODB_URI=

# JWT Secret for signing authentication tokens
# You can generate a strong secret here: https://generate-secret.now.sh/32
JWT_SECRET=

# Resend API Key for sending emails
# Get this from your Resend dashboard.
RESEND_API_KEY=

# Cloudinary credentials for file uploads
# Get these from your Cloudinary dashboard.
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=

# Optional: Set the email for the default admin user.
# When a user registers with this email, they will automatically be assigned the 'admin' role.
ADMIN_EMAIL=
